{
  "name": "naija-agent-core",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "build:packages": "npm run build --workspace=@naija-agent/types && npm run build --workspace=@naija-agent/firebase",
    "build:apps": "npm run build --workspace=@naija-agent/api && npm run build --workspace=@naija-agent/worker",
    "build": "npm run build:packages && npm run build:apps",
    "dev": "npm run dev --workspaces",
    "start": "npm run start --workspaces"
  },
  "devDependencies": {
    "typescript": "^5.3.3",
    "@types/node": "^20.11.0"
  }
}
