rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Basic: allow read/write for now to prevent blocking development.
    // Dashboard uses Server Components (Admin SDK), so it bypasses these rules.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
